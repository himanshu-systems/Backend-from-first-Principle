---
import '@fontsource-variable/geist';
import '@fontsource-variable/geist-mono';
import '@fontsource-variable/source-serif-4';
// Diagram labels only: the inherited artwork was typeset against JetBrains
// Mono's metrics, and every label was positioned by hand to fit its box.
// Substituting a different mono shifts those labels into their neighbours.
import '@fontsource-variable/jetbrains-mono';
import '../styles/tokens.css';
import '../styles/base.css';
import Search from '../components/Search.astro';

interface Props {
  title: string;
  description: string;
  /** Absolute path, used for the canonical URL. */
  path: string;
  /** Absolute path to this page's Open Graph card. Defaults to the site one. */
  image?: string;
}

const { title, description, path, image } = Astro.props;
const canonical = new URL(path, Astro.site);

// A shared link with no image renders as an empty card, and the summary_large_image
// hint below reserves the space whether or not there is anything to put in it.
// The cards are committed under public/og; scripts/generate-og.mjs rebuilds them.
const ogImage = new URL(image ?? '/og/default.png', Astro.site);
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonical} />
  <meta name="referrer" content="no-referrer" />

  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="Backend from First Principles" />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={canonical} />
  <meta property="og:image" content={ogImage} />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:alt" content={title} />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:image" content={ogImage} />

  <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
  <link rel="sitemap" href="/sitemap-index.xml" />
  <link rel="alternate" type="application/rss+xml" title="Backend from First Principles" href="/rss.xml" />

  <!-- Theme must resolve before first paint or the page flashes the wrong
       ground. Inline and blocking on purpose: it is ~15 lines. -->
  <script is:inline>
    (function () {
      var KEY = 'bfp-theme';
      var saved = null;
      try { saved = localStorage.getItem(KEY); } catch (e) { /* storage blocked */ }
      var theme = saved === 'light' || saved === 'dark'
        ? saved
        : (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      document.documentElement.setAttribute('data-theme', theme);
    })();
  </script>
</head>
<body>
  <a class="skip-link" href="#main">Skip to content</a>
  <slot />
  <Search />
</body>
</html>
